# Approved Portal Layout Handoff v10

The generated portal rings are rejected. The closing cinematic must use only the seven existing approved STAARWARDD cover assets in their canonical circular Hub layout, with stable IDs and routes preserved.

| Beat | Visual treatment | Asset rule |
|---|---|---|
| Portal return | The Guardian returns through the original deep portal with a controlled gold ember aura and stabilizes at center. No new destination portals appear in generated media. | Continue to use the fixed original Guardian reference. |
| Glass field | A subtle clear glass-like command field expands around the Guardian, reflecting Toronto light and the Guardian aura. It reads as the Hub environment, not as a new portal design. | Native layered views only; no generated replacement imagery. |
| Gateway formation | The existing approved cover-art gateways materialize in the canonical circle, one at a time, as translucent glass cards that resolve to their unmodified bundled cover images. | `PORTALS` from `portal-data.ts`; no new images, altered covers, IDs, or routes. |
| Interactive state | When all gateways are present, the native `CinematicHub` remains the destination and uses the same orbit, cover art, labels, and portal navigation it already owns. | Preserve the current `CinematicHub` composition and `enter(id)` behavior. |

The review video may composite the actual bundled covers as glass cards over a clean Guardian return shot; it must not use or show generated portal rings.
